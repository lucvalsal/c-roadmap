#include <stdio.h>

int main() {
	unsigned int ui = 100;  // unsigned ints cannot represent negative values
													// it also increases the maximum positive value
													// that can be stored
	short int b;  // it has a size of 2 bytes.
	long int long_int;  // used to increase the storage size and range of data
											// types
	signed int si; // default modifier, it can store both positive and negative
}

$ASLR: Adress Space Layout Randomization.

?How are Pointers related to Arrays?: the name of an array is a pointer to the first element
of the array.

$VLAs: Variable-Length Arrays.

VLAs were introduced in C99 but became optional in C11. If you use an older standard like (C89/C90) or
Microsoft Visual Studio, the code won't compile.
